@{
  Layout = "_Layout";
}

@model Factory.Models.Machine

<div class="mainBox">
  <h2>Add a licensed Enginner to this Machine</h2>
  <hr />
  @using (Html.BeginForm())
  {
      @Html.HiddenFor(model => model.MachineId)

      @Html.Label("Select Engineer")
      @Html.DropDownList("EngineerId", null, "Please Select", htmlAttributes: new{required = "Please select an engineer"})

      <input type="submit" value="Add" class="btn btn-info btn-large" />
  }
  <hr />
  <p>@Html.ActionLink("Show all Machines", "Index",null, null, new { @class = "btn btn-info btn-large" })</p>
</div>